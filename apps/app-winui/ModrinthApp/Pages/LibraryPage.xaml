<Page
	x:Class="ModrinthApp.Pages.LibraryPage"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:models="using:ModrinthApp.Models"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	mc:Ignorable="d">

	<Page.Resources>
		<!-- Filter tab: RadioButton styled as a green pill -->
		<Style x:Key="FilterTabStyle" TargetType="RadioButton">
			<Setter Property="MinWidth" Value="0"/>
			<Setter Property="Padding" Value="0"/>
			<Setter Property="FontFamily" Value="{ThemeResource ContentControlThemeFontFamily}"/>
			<Setter Property="FontSize" Value="{ThemeResource ControlContentThemeFontSize}"/>
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="RadioButton">
						<Grid>
							<Border x:Name="SelectedBg" CornerRadius="20" Background="Transparent"/>
							<Border x:Name="HoverBg" CornerRadius="20"
									Background="{ThemeResource SubtleFillColorSecondaryBrush}"
									Opacity="0"/>
							<TextBlock x:Name="Label"
									   Padding="14 6"
									   Text="{TemplateBinding Content}"
									   FontWeight="SemiBold"
									   HorizontalAlignment="Center"
									   VerticalAlignment="Center"/>
							<VisualStateManager.VisualStateGroups>
								<VisualStateGroup x:Name="CheckStates">
									<VisualState x:Name="Checked">
										<VisualState.Setters>
											<Setter Target="SelectedBg.Background" Value="#1BD96A"/>
											<Setter Target="Label.Foreground" Value="#0A0A0F"/>
										</VisualState.Setters>
									</VisualState>
									<VisualState x:Name="Unchecked"/>
									<VisualState x:Name="Indeterminate"/>
								</VisualStateGroup>
								<VisualStateGroup x:Name="CommonStates">
									<VisualState x:Name="Normal"/>
									<VisualState x:Name="PointerOver">
										<VisualState.Setters>
											<Setter Target="HoverBg.Opacity" Value="1"/>
										</VisualState.Setters>
									</VisualState>
									<VisualState x:Name="Pressed">
										<VisualState.Setters>
											<Setter Target="HoverBg.Background" Value="{ThemeResource SubtleFillColorTertiaryBrush}"/>
											<Setter Target="HoverBg.Opacity" Value="1"/>
										</VisualState.Setters>
									</VisualState>
									<VisualState x:Name="Disabled"/>
								</VisualStateGroup>
							</VisualStateManager.VisualStateGroups>
						</Grid>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>
	</Page.Resources>

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<!-- Filter tabs -->
		<StackPanel Orientation="Horizontal" Padding="16 12 16 4" Spacing="2">
			<RadioButton Content="All instances" GroupName="LibFilter" IsChecked="True"
						 Style="{StaticResource FilterTabStyle}"
						 Checked="FilterTab_Checked" Tag="all"/>
			<RadioButton Content="Modpacks" GroupName="LibFilter"
						 Style="{StaticResource FilterTabStyle}"
						 Checked="FilterTab_Checked" Tag="modpacks"/>
			<RadioButton Content="Servers" GroupName="LibFilter"
						 Style="{StaticResource FilterTabStyle}"
						 Checked="FilterTab_Checked" Tag="servers"/>
			<RadioButton Content="Custom" GroupName="LibFilter"
						 Style="{StaticResource FilterTabStyle}"
						 Checked="FilterTab_Checked" Tag="custom"/>
		</StackPanel>

		<!-- Toolbar: search + sort + group + new -->
		<Grid Grid.Row="1" Padding="16 8" ColumnSpacing="8">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*"/>
				<ColumnDefinition Width="Auto"/>
				<ColumnDefinition Width="Auto"/>
				<ColumnDefinition Width="Auto"/>
				<ColumnDefinition Width="Auto"/>
				<ColumnDefinition Width="Auto"/>
			</Grid.ColumnDefinitions>

			<AutoSuggestBox
				x:Name="SearchBox"
				PlaceholderText="Search"
				QueryIcon="Find"
				VerticalAlignment="Center"
				TextChanged="SearchBox_TextChanged"/>

			<TextBlock Grid.Column="1"
					   Text="Sort by:"
					   VerticalAlignment="Center"
					   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
			<ComboBox x:Name="SortCombo"
					  Grid.Column="2"
					  SelectedIndex="0"
					  VerticalAlignment="Center"
					  SelectionChanged="SortCombo_SelectionChanged">
				<ComboBoxItem Content="Last played"/>
				<ComboBoxItem Content="Name (A–Z)"/>
				<ComboBoxItem Content="Name (Z–A)"/>
				<ComboBoxItem Content="Most mods"/>
			</ComboBox>

			<TextBlock Grid.Column="3"
					   Text="Group by:"
					   VerticalAlignment="Center"
					   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
			<ComboBox x:Name="GroupCombo"
					  Grid.Column="4"
					  SelectedIndex="0"
					  VerticalAlignment="Center">
				<ComboBoxItem Content="None"/>
				<ComboBoxItem Content="Loader"/>
				<ComboBoxItem Content="Game version"/>
			</ComboBox>

			<Button Grid.Column="5"
					Style="{StaticResource AccentButtonStyle}"
					VerticalAlignment="Center"
					Click="NewInstance_Click">
				<StackPanel Orientation="Horizontal" Spacing="8">
					<FontIcon Glyph="&#xE710;" FontSize="14"/>
					<TextBlock Text="New Instance"/>
				</StackPanel>
			</Button>
		</Grid>

		<!-- Instance grid -->
		<ScrollViewer Grid.Row="2" Padding="16 4 16 16">
			<ItemsRepeater x:Name="InstanceGrid"
						   ItemsSource="{x:Bind FilteredInstances}">
				<ItemsRepeater.Layout>
					<UniformGridLayout
						MinItemWidth="256"
						MinColumnSpacing="8"
						MinRowSpacing="8"
						ItemsStretch="Fill"/>
				</ItemsRepeater.Layout>
				<ItemsRepeater.ItemTemplate>
					<DataTemplate x:DataType="models:Instance">
						<Border CornerRadius="12"
								Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
								BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
								BorderThickness="1"
								Padding="16 12"
								Tag="{x:Bind}"
								Tapped="InstanceCard_Tapped"
								IsHitTestVisible="True">
							<Grid ColumnSpacing="12">
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="48"/>
									<ColumnDefinition Width="*"/>
								</Grid.ColumnDefinitions>

								<!-- Instance icon -->
								<Border Width="48" Height="48" CornerRadius="10">
									<Border.Background>
										<SolidColorBrush Color="{x:Bind LoaderColor}"/>
									</Border.Background>
									<FontIcon
										Glyph="&#xE7FC;"
										FontSize="24"
										Foreground="White"
										HorizontalAlignment="Center"
										VerticalAlignment="Center"/>
								</Border>

								<!-- Name + loader / version -->
								<StackPanel Grid.Column="1"
											VerticalAlignment="Center"
											Spacing="4">
									<TextBlock
										Text="{x:Bind Name}"
										FontWeight="SemiBold"
										MaxLines="1"
										TextTrimming="CharacterEllipsis"/>
									<StackPanel Orientation="Horizontal" Spacing="4">
										<FontIcon
											Glyph="&#xE7FC;"
											FontSize="11"
											Foreground="{ThemeResource TextFillColorSecondaryBrush}"
											VerticalAlignment="Center"/>
										<TextBlock
											Text="{x:Bind SubtitleText}"
											Style="{StaticResource CaptionTextBlockStyle}"
											Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
									</StackPanel>
								</StackPanel>
							</Grid>
						</Border>
					</DataTemplate>
				</ItemsRepeater.ItemTemplate>
			</ItemsRepeater>
		</ScrollViewer>
	</Grid>
</Page>
